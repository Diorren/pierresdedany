{% for label,messages in app.flashes %}
    <div class="container-fluid p-0">                    
        <div class="m-0 p-0 alert alert-{{ label }}">
            {% for message in messages %}
                <p class="text-center mt-1 mb-1">{{ message | raw }}</p>
            {% endfor %}                    
        </div>
    </div>            
{% endfor %}
<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-secondary">
        <a class="navbar-brand" href="{{ path('app_home') }}">Accueil</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_home') }}">Tableau de bord</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_products_home') }}">Produits</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_categories_home') }}"><i class=""></i>Categories</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_users') }}">Utilisateurs</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item mr-5">
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login')}}" >Connexion</a>
                    </li>
                {% endif %}                    
            </ul>
        </div>
    </nav>
</header>

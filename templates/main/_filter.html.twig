{{ form_start(form, {attr: {class: 'filter'}}) }}
    <p>
        {{ form_widget(form.q) }}
    </p>

    <h4>Catégories</h4>
    {{ form_row(form.categories) }}

    <h4>Prix</h4>
    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.min) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.max) }}
        </div>
    </div>
    <div id="price-slider" class="mb-3" data-min="{{ min }}" data-max="{{ max }}"></div>

    <h4>Promotions</h4>
    {{ form_widget(form.promo) }}

    <button type="submit" class="btn filtrer w-100">Filtrer</button>

{{ form_end(form) }}
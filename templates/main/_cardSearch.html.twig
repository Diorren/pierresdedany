{% set url = path('products_single',{slug: product.slug}) %}    
    <div class="card-body article">
        {% if product.promo == true %}
            <p class="text-center mb-1" id="promo" style="color:red;">Promotion</p>               
        {% endif %}
        <a href="{{url}}">
            {% if product.images is not empty %}
                <img id="collection" src="{{ asset('uploads/'~ product.images[0].name) }}" alt="{{product.name}}"
                style="height:200px;width:100%;display:block;">
            {% endif %}
        </a>
        <h3 class="text-center mt-2 title-bestsellers">{{product.name}}</h3>
        <h5>Catégories: {{product.categories.name}}</h5>
        <p class="font-weight-bold">Prix: {{ product.price | number_format(0,',',' ') }} €</p>
        <p>Contenu: {{product.content | raw}}</p>
        {% for c in product.categories %}
            <em class="text-muted text-primary text-italic">#{{ c.name }}</em>
        {% endfor %}
        <a href="{{ path('users_payment', {'slug': product.slug}) }}" type="button" class="btn buttonPaid">Acheter</a>
    </div>
   